version: '3'
services:
  flask:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: flask
    ports:
      - "5000:5000"
    restart: always
    command: sh -c "/app/start.sh"
    networks:
      - my_bridge_network

networks:
  my_bridge_network:
    driver: bridge