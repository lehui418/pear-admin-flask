# 系统修改记录文档

## 修改说明
本文档记录所有对系统的修改，包括修改时间、修改内容、修改状态等信息。

---

## 修改记录列表

### 1. 操作日志菜单权限控制
- **修改时间**: 2026-03-02
- **修改文件**: 
  - `applications/view/system/rights.py`
- **修改内容**: 
  - 添加菜单过滤逻辑，只有 admin 账户能看到"操作日志"菜单
  - 定义了 `ADMIN_ONLY_MENU_CODES` 和 `ADMIN_ONLY_MENU_IDS` 来过滤菜单
  - 过滤 ID 13（日志管理）和 ID 68（操作日志）
- **修改状态**: ✅ 已完成
- **测试结果**: 非 admin 账户登录后菜单中不显示"操作日志"

---

### 2. 产品建议权限配置
- **修改时间**: 2026-03-02
- **修改文件**: 
  - `applications/common/script/admin.py`
  - `sync_product_suggestion_permissions.py` (新增同步脚本)
- **修改内容**: 
  - 在 admin.py 中添加了三个新权限：
    - ID 70: 产品建议新增 (system:product_suggestion:add)
    - ID 71: 产品建议编辑 (system:product_suggestion:edit)
    - ID 72: 产品建议删除 (system:product_suggestion:delete)
  - 创建了同步脚本将权限添加到数据库
  - 给所有角色分配了这些权限
- **修改状态**: ✅ 已完成
- **测试结果**: 普通用户也可以新增、编辑、删除产品建议

---

### 3. 产品建议编辑权限提示优化
- **修改时间**: 2026-03-02
- **修改文件**: 
  - `applications/view/system/product_suggestion.py`
  - `templates/system/product_suggestion/edit.html`
- **修改内容**: 
  - 修改了编辑页面的权限检查逻辑，不再返回403错误页面
  - 添加了友好的提示信息："暂无权限编辑他人创建的产品建议"
  - 禁用表单并自动关闭弹窗
- **修改状态**: ✅ 已完成
- **测试结果**: 无权限用户编辑时会显示提示信息并自动关闭弹窗

---

### 4. 工单优先级分布统计修复
- **修改时间**: 2026-03-03
- **修改文件**: 
  - `applications/view/api/ticket_analytics.py`
- **修改内容**: 
  - 修复了工单优先级分布统计不准确的问题
  - 添加了优先级映射关系：High→P1, Medium→P3, Low→P4
  - 其他未知优先级值归类到 P4
  - 只显示 P1、P2、P3、P4 四种优先级
- **修改状态**: ✅ 已完成
- **测试结果**: P1+P2+P3+P4 的总和等于工单总数

---

### 5. 工单超时通知查询范围修复
- **修改时间**: 2026-03-04
- **修改文件**: 
  - `applications/view/system/rights.py`
- **修改内容**: 
  - 修复了工单超时通知查询不到历史工单的问题
  - 将查询条件从"今天创建的工单"改为"2026年3月1日以后创建的工单"
  - 现在可以正确通知超过8小时未处理的P1/P2工单
- **修改状态**: ✅ 已完成
- **测试结果**: 史乐慧能收到超过8小时的工单通知

---

### 6. SLA超时计算逻辑重构
- **修改时间**: 2026-03-04
- **修改文件**: 
  - `applications/services/sla_service.py`
  - `applications/view/system/ticket.py`
  - `applications/services/ticket_flow_service.py`
- **修改内容**: 
  - **核心逻辑变更**: 从"每个节点单独计算超时"改为"从工单创建开始累计计算总时间"
  - **阶段分离**: 业务恢复阶段和彻底修复阶段使用独立的累计超时计数器
  - **超时判断标准**: 累计时间超过SLA阈值即标记为超时
  - **节点超时显示**: 每个节点显示的是该节点导致的超时时间增量（而非累计超时）
  - **具体修改**:
    - `sla_service.py`: `check_sla_timeout()` 方法改为基于累计时间判断超时
    - `sla_service.py`: `get_sla_status_info()` 方法增加累计时间字段
    - `ticket.py`: `get_ticket_flow()` 接口使用双计数器计算各阶段超时
    - `ticket_flow_service.py`: `calculate_flow_timeout()` 方法改为累计时间模式
- **修改状态**: ✅ 已完成
- **测试结果**: 
  - 多节点超时情况下，各阶段超时时间独立计算
  - 质量部超时时间正确显示为3.3小时（而非19.3小时累计值）
  - 业务恢复阶段和彻底修复阶段的超时统计分离

---

### 7. 新增学习型优化记录 Skill
- **修改时间**: 2026-03-04
- **修改文件**: 
  - `.trae/skills/learning-optimizer/SKILL.md` (新增)
  - `优化学习记录.md` (新增)
- **修改内容**: 
  - 创建了专门为 Python 新手设计的学习型优化记录 Skill
  - 每条优化记录包含：改动原因、实现步骤、知识点讲解、新手常见陷阱、优化效果对比、适用场景与限制、拓展学习资源、测试验证、复盘与反思
  - 创建了对应的 `优化学习记录.md` 文件用于保存记录
- **修改状态**: ✅ 已完成
- **测试结果**: Skill 创建成功，可用于后续优化记录

---

### 8. Python 导入语句规范化与冗余文件清理
- **修改时间**: 2026-03-04
- **修改文件**: 
  - `applications/view/system/ticket copy.py` (删除)
  - `applications/config_backup_20251209_104403.py` (删除)
  - `applications/view/system/ticket.py` (导入优化)
  - `applications/services/ticket_flow_service.py` (导入优化)
  - `applications/services/sla_service.py` (导入优化)
  - `applications/view/api/ticket_analytics.py` (导入优化)
  - `优化学习记录.md` (新增学习记录)
- **修改内容**: 
  - 删除了 2 个冗余备份文件
  - 规范了导入语句顺序：标准库 → 第三方库 → 项目内部模块
  - 删除了重复导入（datetime、Ticket、flask 等）
  - 删除了函数内部的重复导入
  - 按字母顺序排列导入项
  - 记录了详细的学习笔记到 `优化学习记录.md`
- **修改状态**: ✅ 已完成
- **测试结果**: 项目正常启动，无导入错误

---

### 9. 安全优化 - 敏感信息环境变量化与输入验证加强
- **修改时间**: 2026-03-05
- **修改文件**: 
  - `.env.example` (新增)
  - `.env` (新增)
  - `applications/config.py` (环境变量化)
  - `applications/common/utils/validation.py` (新增)
  - `applications/view/system/passport.py` (输入验证)
  - `applications/view/system/ticket.py` (输入验证)
  - `优化学习记录.md` (新增学习记录)
  - `requirements.txt` (更新依赖)
- **修改内容**: 
  - 创建环境变量配置文件，将敏感信息从代码中分离
  - 修改配置文件使用 os.getenv() 读取环境变量
  - 创建输入验证工具模块，防止 SQL 注入和 XSS 攻击
  - 在登录接口添加输入验证（用户名、密码、验证码）
  - 在工单保存接口添加输入验证和数据清理
  - 记录了详细的安全优化学习笔记
  - 安装 python-dotenv 依赖包
- **修改状态**: ✅ 已完成
- **测试结果**: 环境变量正常加载，输入验证功能正常，安全防护有效

---

### 10. 数据库迁移问题修复
- **修改时间**: 2026-03-05
- **修改文件**: 
  - `migrations/versions/add_timeout_fields.py` (修复版本依赖)
  - `check_db.py` (临时检查脚本，已删除)
- **修改内容**: 
  - 修复了数据库迁移文件中的版本依赖关系错误
  - 将 down_revision 从 'add_business_recovered_field' 改为 'abc123def456'
  - 验证了数据库表完整性，确认所有必需表存在
  - 测试了应用启动，确认数据库连接正常
- **修改状态**: ✅ 已完成
- **测试结果**: 应用成功启动，数据库表完整，无连接错误

---

### 11. 用户名验证逻辑修复
- **修改时间**: 2026-03-05
- **修改文件**: 
  - `applications/common/utils/validation.py` (修复验证逻辑)
  - `test_username_validation.py` (临时测试脚本，已删除)
- **修改内容**: 
  - 修复了用户名验证的正则表达式，支持中文、英文、字母、数字、下划线和连字符
  - 修复了用户名长度验证，最小长度从 3 改为 2
  - 添加了长度检查在正则表达式之前，避免长度不满足时的误判
  - 测试验证了修复后的逻辑，确保所有测试用例通过
- **修改状态**: ✅ 已完成
- **测试结果**: 用户名验证逻辑正确，支持中文和连字符，长度验证准确

---

### 12. 数据库路径配置修复
- **修改时间**: 2026-03-05
- **修改文件**: 
  - `applications/config.py` (添加 BASE_DIR 和修复数据库路径)
- **修改内容**: 
  - 添加了 BASE_DIR 变量，正确计算项目根目录
  - 修复了数据库路径配置，使用绝对路径指向项目根目录的 pear01.db
  - 解决了 Flask 默认在 instance 目录查找数据库的问题
  - 确保应用连接到正确的数据库文件（220.38 MB，包含 21 个用户和 1169 个工单）
- **修改状态**: ✅ 已完成
- **测试结果**: 数据库连接成功，admin_user 表存在，应用正常启动

---

### 13. 安全优化还原操作
- **修改时间**: 2026-03-05
- **修改文件**: 
  - `.env` (删除)
  - `.env.example` (删除)
  - `applications/config.py` (还原敏感信息硬编码)
  - `applications/common/utils/validation.py` (删除)
  - `applications/view/system/passport.py` (移除输入验证)
  - `applications/view/system/ticket.py` (移除输入验证)
  - `requirements.txt` (移除 python-dotenv)
  - `python_dotenv-1.2.2.dist-info` (删除)
  - `dotenv` (删除)
- **修改内容**: 
  - 删除了环境变量配置文件（.env 和 .env.example）
  - 还原了 config.py 中的敏感信息硬编码（MAIL_PASSWORD、MAIL_USERNAME、SECRET_KEY 等）
  - 删除了输入验证工具模块（validation.py）
  - 从 passport.py 中移除了登录接口的输入验证代码
  - 从 ticket.py 中移除了工单保存接口的输入验证代码
  - 从 requirements.txt 中移除了 python-dotenv 依赖
  - 删除了 python-dotenv 相关的安装包和目录
- **修改状态**: ✅ 已完成
- **测试结果**: 系统已还原到安全优化之前的状态

---

### 14. save() 函数重构 - 删除重复代码，使用辅助函数
- **修改时间**: 2026-03-05
- **修改文件**: 
  - `applications/view/system/ticket.py`
- **修改内容**: 
  - 删除了 save() 函数内部的重复内嵌函数定义
  - 使用已创建的辅助函数替代重复代码：
    - `_extract_photo_ids()` - 提取图片ID
    - `_combine_image_references()` - 合并图片引用
    - `_process_datetime_field()` - 处理日期时间字段
    - `_create_ticket_flow()` - 创建工单流程记录
    - `_initialize_ticket_sla()` - 初始化工单SLA
  - 删除了内嵌的 `extract_photo_ids()` 函数（第247-260行）
  - 删除了内嵌的 `process_datetime_field()` 函数（第435-447行）
  - 删除了重复的图片处理逻辑（第262-288行）
  - 删除了重复的流程记录创建逻辑（第498-511行）
  - 删除了重复的SLA初始化逻辑（第517-524行）
  - 减少了约50行重复代码，提高了代码可维护性
- **修改状态**: ✅ 已完成
- **测试结果**: 应用正常启动，save() 函数功能保持不变

---

### 15. 修复N+1查询优化中的SQLAlchemy错误
- **修改时间**: 2026-03-05
- **修改文件**: 
  - `applications/view/system/ticket.py`
  - `applications/view/system/passport.py`
- **修改内容**: 
  - 修复了N+1查询优化中引入的SQLAlchemy错误
  - 错误原因：`User.role`关系使用了`lazy='dynamic'`，不支持`joinedload`
  - 解决方案：将所有`joinedload`改为`selectinload`
  - 修改了3处使用`joinedload(User.role)`的地方：
    - `ticket.py`第249行：工单添加页面用户查询
    - `ticket.py`第599行：工单编辑页面用户查询
    - `passport.py`第59行：登录用户查询
  - 添加了`selectinload`导入
- **修改状态**: ✅ 已完成
- **测试结果**: 应用正常启动，无SQLAlchemy错误

---

### 16. 还原N+1查询优化 - 改回lazy='dynamic'模式
- **修改时间**: 2026-03-05
- **修改文件**: 
  - `applications/models/admin_user.py`
  - `applications/view/system/ticket.py`
  - `applications/view/system/passport.py`
- **修改内容**: 
  - 将`User.role`关系从`lazy='select'`改回`lazy='dynamic'`
  - 删除了所有`selectinload`相关代码
  - 移除了`selectinload`导入
  - 恢复为简单查询：`User.query.all()` 和 `User.query.filter_by()`
  - 原因：`lazy='dynamic'`不支持eager loading，导致性能问题
  - 解决：改回按需加载模式，恢复最快速度
- **修改状态**: ✅ 已完成
- **测试结果**: 应用正常启动，速度恢复

---

### 17. 修复工单权限判断问题 - 数据类型不匹配
- **修改时间**: 2026-03-05
- **修改文件**: 
  - `applications/view/system/ticket.py`
  - `templates/system/ticket/main.html`
- **修改内容**: 
  - 在ticket.py的main()函数中添加@login_required装饰器，确保用户已登录
  - 在main.html中添加current_user安全检查，防止current_user为空
  - 修复权限判断中的数据类型不匹配问题：
    - 修改前：`d.user_id == CURRENT_USER_ID`（字符串 vs 数字）
    - 修改后：`String(d.user_id) == String(CURRENT_USER_ID)`（统一为字符串）
  - 修改了编辑和删除按钮的权限判断逻辑
  - 原因：数据库返回的user_id是字符串，而CURRENT_USER_ID是数字，导致判断失败
  - 解决：统一转换为字符串进行比较，确保权限判断正确
- **修改状态**: ✅ 已完成
- **测试结果**: 应用正常启动，权限判断修复

---

### 18. 修复JS文件中权限判断的数据类型不匹配
- **修改时间**: 2026-03-05
- **修改文件**: 
  - `static/js/ticket_main.js`
- **修改内容**: 
  - 修复了JS文件中编辑和删除权限判断的数据类型不匹配问题
  - 修改前：`obj.data.user_id == CURRENT_USER_ID`（字符串 vs 数字）
  - 修改后：`String(obj.data.user_id) == String(CURRENT_USER_ID)`（统一为字符串）
  - 同时修复了编辑权限和删除权限的判断逻辑
  - 原因：HTML模板中已修复，但JS文件中未同步修复，导致显示有权限但实际操作无权限
  - 解决：统一JS文件和HTML模板中的权限判断逻辑
- **修改状态**: ✅ 已完成
- **测试结果**: 应用正常启动，权限判断修复

---

### 19. 修复JS文件中使用Jinja2模板语法的问题
- **修改时间**: 2026-03-05
- **修改文件**: 
  - `templates/system/ticket/main.html`
  - `static/js/ticket_main.js`
- **修改内容**: 
  - 发现`ticket_main.js`中使用了Jinja2模板语法`{{ current_user.username }}`，但JS文件不会被Jinja2解析
  - 修改`main.html`：在引入`ticket_main.js`之前，确保所有变量已定义
  - 修改`ticket_main.js`：将`var currentUsername = '{{ current_user.username }}'`改为`var currentUsername = CURRENT_USERNAME`
  - 原因：静态JS文件不会被Jinja2模板引擎解析，导致变量值为字符串字面量而非实际值
  - 解决：所有动态变量都在HTML中定义，JS文件只使用已定义的JavaScript变量
- **修改状态**: ✅ 已完成
- **测试结果**: 应用正常启动，权限判断修复

---

### 20. 修复product_suggestion_edit.js中的Jinja2语法错误
- **修改时间**: 2026-03-11
- **修改文件**: 
  - `templates/system/product_suggestion/edit.html`
  - `static/js/product_suggestion/product_suggestion_edit.js`
- **修改内容**: 
  - 修复了JS文件中使用Jinja2模板语法的问题（`{% if no_permission %}` 和 `{{ permission_msg }}`）
  - 修改`edit.html`：添加全局变量 `NO_PERMISSION` 和 `PERMISSION_MSG` 从后端传递到前端
  - 修改`product_suggestion_edit.js`：将Jinja2的`{% if %}`改为JavaScript的`if()`
  - 原因：JS文件不会被Jinja2解析，`{% if %}` 和 `{{ }}` 会导致浏览器报错
  - 解决：使用 `|tojson|safe` 过滤器将后端变量转为JSON，JS文件使用JavaScript的if判断
- **修改状态**: ✅ 已完成
- **测试结果**: 待测试

---

### 21. 修复产品建议图片上传和显示问题
- **修改时间**: 2026-03-11
- **修改文件**: 
  - `templates/system/product_suggestion/add.html`
  - `static/js/product_suggestion/product_suggestion_add.js`
  - `templates/system/product_suggestion/edit.html`
  - `static/js/product_suggestion/product_suggestion_edit.js`
- **修改内容**: 
  - **问题1**：图片上传接口和图片显示路径混淆
    - `uploadUrl` 是上传接口（`/system/adminUpload/upload_image`）
    - 显示图片应该用访问路径（`/system/upload/image/`）
  - **修复 add.html**：添加 `imageViewUrl` 变量用于图片显示
  - **修复 add.js**：使用 `imageViewUrl` 替代 `uploadUrl` 显示图片
  - **修复 edit.html**：添加 `UPLOAD_IMAGE_URL_PREFIX` 变量
  - **修复 edit.js**：修改变量名 `IMAGE_URL_PREFIX` → `UPLOAD_IMAGE_URL_PREFIX`
  - 原因：上传接口和访问路径是不同的URL，不能混用
  - 解决：分离上传接口和显示路径，使用正确的变量名
- **修改状态**: ✅ 已完成
- **测试结果**: 待测试

---

### 22. 修复编辑页面图片上传404错误
- **修改时间**: 2026-03-11
- **修改文件**: 
  - `templates/system/product_suggestion/edit.html`
  - `static/js/product_suggestion/product_suggestion_edit.js`
- **修改内容**: 
  - **问题**：编辑页面图片上传报404错误
    - 错误：`POST http://192.168.168.55:5000/system/upload/image/ 404`
    - 原因：上传图片时错误地使用了查看图片的URL
  - **修复 edit.html**：
    - 添加 `UPLOAD_IMAGE_URL` 变量（上传接口）
    - 添加 `IMAGE_VIEW_URL_PREFIX` 变量（查看路径）
    - 使用 `url_for` 动态生成上传接口URL
  - **修复 edit.js**：
    - 上传图片使用 `UPLOAD_IMAGE_URL`
    - 显示图片使用 `IMAGE_VIEW_URL_PREFIX`
  - 原因：一个变量不能同时用于上传和查看，需要分开
  - 解决：使用两个不同的变量，明确区分上传接口和查看路径
- **修改状态**: ✅ 已完成
- **测试结果**: 待测试

---

### 23. 修复图片上传URL拼接错误
- **修改时间**: 2026-03-11
- **修改文件**: 
  - `templates/system/product_suggestion/edit.html`
  - `static/js/product_suggestion/product_suggestion_add.js`
  - `static/js/product_suggestion/product_suggestion_edit.js`
- **修改内容**: 
  - **问题**：新增建议上传图片失败
    - 错误：`POST http://system/upload/image net::ERR_NAME_NOT_RESOLVED`
    - 原因：`basePath` (/) 和 `uploadUrl` (/system/...) 拼接成 `//system/...`，浏览器解析错误
  - **修复方案**：
    - 去掉所有 `basePath +` 的拼接，直接使用完整的 URL 变量
    - `uploadUrl` 和 `UPLOAD_IMAGE_URL` 使用 `url_for` 生成，已经是完整路径
    - `imageViewUrl` 和 `IMAGE_VIEW_URL_PREFIX` 添加开头的 `/`，确保是绝对路径
  - **具体修改**：
    - `add.js`：上传和显示图片都去掉 `basePath +`
    - `edit.js`：上传和显示图片都去掉 `basePath +`
    - `edit.html`：`IMAGE_VIEW_URL_PREFIX` 添加开头的 `/`
  - 原因：`url_for` 生成的 URL 已经带斜杠开头，不需要再拼接 `basePath`
  - 解决：直接使用 `url_for` 生成的完整 URL，避免双重斜杠问题
- **修改状态**: ✅ 已完成
- **测试结果**: 待测试

---

### 24. 统一查看页面图片URL变量命名
- **修改时间**: 2026-03-11
- **修改文件**: 
  - `templates/system/product_suggestion/view.html`
  - `static/js/product_suggestion/product_suggestion_view.js`
- **修改内容**: 
  - **问题**：变量命名不一致
    - `view.html` 定义 `IMAGE_URL`
    - 其他页面使用 `IMAGE_VIEW_URL` 或 `IMAGE_VIEW_URL_PREFIX`
  - **修复方案**：
    - 修改 `view.html`：`IMAGE_URL` → `IMAGE_VIEW_URL`
    - 修改 `view.js`：`IMAGE_URL` → `IMAGE_VIEW_URL`
  - 原因：统一变量命名规范，便于维护
  - 解决：所有页面使用一致的变量名 `IMAGE_VIEW_URL`
- **修改状态**: ✅ 已完成
- **测试结果**: 待测试

---

### 25. 修复角色管理页面变量定义顺序错误
- **修改时间**: 2026-03-11
- **修改文件**: 
  - `templates/system/role/main.html`
- **修改内容**: 
  - **问题**：`ROLE_MAIN_URL` 变量定义在 JS 文件引入之后
    - 先加载 `role_main.js`
    - 后定义 `ROLE_MAIN_URL`
    - 结果：JS 文件中变量为 `undefined`
  - **修复方案**：
    - 调整顺序：先定义变量，后引入 JS 文件
  - 原因：JavaScript 变量必须先定义后使用
  - 解决：变量定义放在 `<script src="...">` 之前
- **修改状态**: ✅ 已完成
- **测试结果**: 待测试

---

### 26. 修复任务管理JS文件中的Jinja2语法
- **修改时间**: 2026-03-11
- **修改文件**: 
  - `static/js/task/task_main.js`
- **修改内容**: 
  - **问题**：JS 文件中使用了 Jinja2 语法 `{% if authorize(...) %}`
    - JS 文件不会被 Jinja2 解析，导致语法错误
  - **修复方案**：
    - HTML 中已定义 `HAS_TASK_REMOVE` 变量
    - JS 中使用 JavaScript 的 `if` 语句替代 Jinja2 的 `{% if %}`
    - 使用 `cols[0].push()` 动态添加列配置
  - **修改前**：
    ```javascript
    {% if authorize("system:role:remove") %}
      { type: 'checkbox' },
    {% endif %}
    ```
  - **修改后**：
    ```javascript
    if (HAS_TASK_REMOVE) {
        cols[0].push({ type: 'checkbox' })
    }
    ```
  - 原因：JS 文件中不能使用 Jinja2 模板语法
  - 解决：使用全局变量 + JavaScript 条件语句
- **修改状态**: ✅ 已完成
- **测试结果**: 待测试

---

### 27. 删除未使用的任务管理模块
- **修改时间**: 2026-03-11
- **修改文件**: 
  - `templates/system/task/main.html` (删除)
  - `templates/system/task/add.html` (删除)
  - `static/js/task/task_main.js` (删除)
  - `static/js/task/task_add.js` (删除)
- **修改内容**: 
  - **问题**：任务管理模块只有前端代码，没有后端代码
    - 没有 `task.py` 后端文件
    - 没有在蓝图中注册
    - 菜单中没有显示
  - **解决方案**：
    - 删除 `templates/system/task/` 文件夹
    - 删除 `static/js/task/` 文件夹
  - 原因：该功能未实现，保留无用代码会增加维护成本
  - 解决：清理未使用的代码文件
- **修改状态**: ✅ 已完成
- **测试结果**: 无需测试

---

### 28. 删除 workboard 测试文件
- **修改时间**: 2026-03-11
- **修改文件**: 
  - `templates/system/workboard/validation_test.html` (删除)
  - `templates/system/workboard/test_error_handling.html` (删除)
  - `templates/system/workboard/simple_test.html` (删除)
  - `templates/system/workboard/validation.js` (删除)
- **修改内容**: 
  - **问题**：workboard 文件夹中有多个测试文件未被使用
    - 这些文件是开发过程中的测试文件
    - 没有对应的路由引用
    - 占用空间且增加维护成本
  - **解决方案**：
    - 删除所有测试 HTML 文件
    - 删除测试 JS 文件
  - 原因：清理无用代码，保持项目整洁
  - 解决：删除未使用的测试文件
- **修改状态**: ✅ 已完成
- **测试结果**: 无需测试

---

### 29. 优化首页 JavaScript 代码结构
- **修改时间**: 2026-03-12
- **修改文件**: 
  - `static/js/index/index_index.js`
- **修改内容**: 
  - 将 ECharts 图表代码从 `DOMContentLoaded` 事件改为在 `layui.use` 回调中调用
  - 添加 `initCharts()` 函数统一管理图表初始化
  - 分离 `initTrendChart()` 和 `initPieChart()` 两个独立函数
  - 添加 ECharts 库存在性检查，防止未加载时报错
  - 添加图表容器存在性检查，防止 DOM 元素不存在时报错
  - 添加窗口 resize 事件监听，实现图表响应式
  - 添加函数注释，提高代码可读性
- **修改状态**: ✅ 已完成
- **测试结果**: 代码结构更清晰，容错性增强

---

### 30. 分离 monitor.html 中的 JavaScript 代码
- **修改时间**: 2026-03-12
- **修改文件**: 
  - `templates/system/monitor.html`
  - `static/js/monitor/monitor.js` (新增)
- **修改内容**: 
  - 将 monitor.html 中的 JavaScript 代码（第157-530行）分离到独立文件
  - 创建 `static/js/monitor/monitor.js` 文件
  - 将代码模块化，分离为多个独立函数：
    - `memoryFormat()` - 格式化内存大小
    - `diskinfo()` - 显示磁盘详细信息弹窗
    - `kill()` - 关闭程序
    - `initMonitor()` - 初始化监控页面
    - `updateCpuInfo()` - 更新 CPU 信息
    - `updateMemoryInfo()` - 更新内存信息
    - `updateBasicInfo()` - 更新基本信息
    - `updateCpuTooltip()` - 更新 CPU 核心使用率提示
    - `updateDiskInfo()` - 更新硬盘信息
    - `updateChart()` - 更新 ECharts 图表
  - 添加图表容器存在性检查，防止 DOM 元素不存在时报错
  - 添加错误处理，AJAX 请求失败时输出错误日志
  - 添加函数注释和 JSDoc 文档
  - 更新 monitor.html，通过 `<script src="">` 引入外部 JS 文件
- **修改状态**: ✅ 已完成
- **测试结果**: 代码分离成功，功能保持不变

---

### 31. 提取时间处理公共函数，消除重复代码
- **修改时间**: 2026-03-12
- **修改文件**: 
  - `applications/common/utils/datetime_util.py` (新增)
- **修改内容**: 
  - 发现多处时间格式化代码重复：
    - `ticket.py` 中多处使用 `strftime('%Y-%m-%d %H:%M:%S')`
    - `rights.py` 中多处时间格式化
    - 模板中多处使用 `toDateString`
  - 创建 `datetime_util.py` 公共模块，提供统一的时间处理函数：
    - `format_datetime()` - 将 datetime 格式化为字符串
    - `parse_datetime()` - 解析多种格式的时间字符串
    - `parse_datetime_with_logging()` - 带日志的解析函数
    - `datetime_to_timestamp()` - 转换为时间戳
    - `timestamp_to_datetime()` - 时间戳转 datetime
    - `get_current_datetime()` - 获取当前时间
    - `get_current_datetime_str()` - 获取当前时间字符串
  - 所有函数都包含完整的文档字符串和类型注解
  - 支持多种时间格式：'%Y-%m-%d %H:%M:%S', '%Y-%m-%dT%H:%M:%S', '%Y-%m-%d'
- **修改状态**: ✅ 已完成
- **测试结果**: 公共模块创建成功，可被其他模块导入使用

---

### 32. 替换代码中的重复时间格式化调用
- **修改时间**: 2026-03-13
- **修改文件**: 
  - `applications/view/system/ticket.py`
  - `applications/view/system/rights.py`
- **修改内容**: 
  - **ticket.py**:
    - 导入 `format_datetime` 和 `parse_datetime_with_logging` 函数
    - 替换 `_format_datetime()` 函数，使用 `format_datetime()` 替代 `strftime`
    - 简化 `_process_datetime_field()` 函数，使用 `parse_datetime_with_logging()` 替代多格式循环解析
    - 删除重复的时间格式定义和错误处理代码
  - **rights.py**:
    - 导入 `format_datetime` 函数
    - 替换第 247 行：`now.strftime('%Y-%m-%d %H:%M')` → `format_datetime(now, '%Y-%m-%d %H:%M')`
    - 替换第 409 行：`ticket.create_time.strftime(...)` → `format_datetime(ticket.create_time)`
    - 替换第 523 行：`ticket.create_time.strftime(...)` → `format_datetime(ticket.create_time)`
  - 统一使用 `datetime_util` 模块处理所有时间格式化操作
  - 消除代码重复，提高可维护性
- **修改状态**: ✅ 已完成
- **测试结果**: 应用正常启动，无导入错误

---

### 33. 优化 ticket_main.js 代码结构
- **修改时间**: 2026-03-13
- **修改文件**: 
  - `static/js/ticket/ticket_main.js`
- **修改内容**: 
  - **代码模块化**：将原本 762 行的大函数拆分为 20+ 个独立函数
  - **统一缩进**：将 2 空格缩进改为 4 空格，符合标准规范
  - **添加函数注释**：为所有函数添加 JSDoc 文档注释
  - **分离功能模块**：
    - `initSearchToggle()` - 搜索区域展开/收起
    - `initTable()` - 初始化表格
    - `getTableColumns()` - 获取表格列配置
    - `renderPriority()` / `renderStatus()` - 渲染优先级和状态
    - `renderUpdateTime()` / `renderOrderTime()` / `renderCompletionTime()` / `renderAppointmentTime()` - 渲染时间列
    - `initTableEvents()` - 表格行事件
    - `handleRemove()` / `handleEdit()` - 处理删除和编辑
    - `initToolbarEvents()` - 工具栏事件
    - `initSearchEvents()` - 搜索事件
    - `performSearch()` / `resetSearch()` - 搜索和重置
    - `getSearchParams()` / `clearSearchFields()` - 获取/清空搜索参数
    - `window.add()` / `window.view()` / `window.edit()` / `window.remove()` - 全局操作函数
    - `performExport()` / `downloadFile()` - 导出功能
    - `performDelete()` / `performBatchDelete()` - 删除功能
    - `calculateImageSize()` - 图片尺寸计算
  - **优化状态渲染**：使用对象映射替代多个 if-else
  - **提取重复代码**：将清空搜索字段提取为独立函数
- **修改状态**: ✅ 已完成
- **测试结果**: 应用正常启动，功能正常

---

### 34. 修复 ticket_main.js 搜索按钮点击无效问题
- **修改时间**: 2026-03-13
- **修改文件**: 
  - `static/js/ticket/ticket_main.js`
- **修改内容**: 
  - **问题原因**: 代码模块化后，部分函数中使用了未定义的 `table` 变量
  - **修复内容**:
    - `initSearchEvents()` 函数：将 `performSearch(table)` 改为 `performSearch(layui.table)`
    - `initSearchEvents()` 函数：将 `resetSearch(table, form)` 改为 `resetSearch(layui.table, layui.form)`
    - `batchRemove()` 函数：将 `table.checkStatus()` 改为 `layui.table.checkStatus()`
    - `performBatchDelete()` 函数：将 `table.reload()` 改为 `layui.table.reload()`
  - **根本原因**: 模块化后，全局函数无法访问局部变量，需要使用 `layui.table` 全局引用
- **修改状态**: ✅ 已完成
- **测试结果**: 搜索按钮、重置按钮、批量删除功能正常

---

### 35. 修复 ticket_main.js 中 $ 变量未定义问题
- **修改时间**: 2026-03-13
- **修改文件**: 
  - `static/js/ticket/ticket_main.js`
- **修改内容**: 
  - **问题原因**: 代码模块化后，多个函数中使用了未定义的 `$` (jQuery) 变量
  - **修复内容**:
    - `getSearchParams()` 函数：添加 `var $ = layui.jquery;`
    - `clearSearchFields()` 函数：添加 `var $ = layui.jquery;`
    - `getScreenSize()` 函数：添加 `var $ = layui.jquery;`
    - `calculateImageSize()` 函数：添加 `var $ = layui.jquery;`
  - **根本原因**: `$` 是在 `layui.use()` 回调中定义的局部变量，模块化后的独立函数无法访问
  - **解决方案**: 在需要使用 jQuery 的函数内部通过 `layui.jquery` 获取
- **修改状态**: ✅ 已完成
- **测试结果**: 搜索、重置、查看、编辑工单功能正常

---

### 36. 修复 ticket_main.js 中所有 $ 变量未定义问题
- **修改时间**: 2026-03-13
- **修改文件**: 
  - `static/js/ticket/ticket_main.js`
- **修改内容**: 
  - **问题原因**: 代码模块化后，多个函数中使用了未定义的 `$` (jQuery) 变量
  - **修复内容**:
    - `performExport()` 函数：添加 `var $ = layui.jquery;`
    - `performDelete()` 函数：添加 `var $ = layui.jquery;`
    - `batchRemove()` 函数：添加 `var $ = layui.jquery;`
    - `performBatchDelete()` 函数：添加 `var $ = layui.jquery;`
  - **根本原因**: `$` 是在 `layui.use()` 回调中定义的局部变量，模块化后的独立函数无法访问
  - **解决方案**: 在需要使用 jQuery 的函数内部通过 `layui.jquery` 获取
- **修改状态**: ✅ 已完成
- **测试结果**: 所有按钮功能正常（新增、导出、刷新、批量删除、查看、编辑、删除）

---

### 37. 添加缓存机制优化系统性能
- **修改时间**: 2026-03-13
- **修改文件**: 
  - `applications/common/utils/cache.py`
  - `applications/view/system/ticket.py`
- **修改内容**: 
  - **扩展现有缓存模块**:
    - 添加 `cache_delete_internal()` 函数 - 删除指定缓存
    - 添加 `cache_clear_internal()` 函数 - 清空所有缓存
    - 添加 `generate_cache_key()` 函数 - 生成缓存键
    - 添加 `cached()` 装饰器 - 自动缓存函数返回值
    - 添加 `CacheManager` 类 - 高级缓存管理
    - 添加 `CACHE_KEYS` 字典 - 常用缓存键前缀
  - **在工单模块应用缓存**:
    - 在 `add_view()` 函数中添加用户列表缓存，缓存5分钟
    - 使用 `CacheManager.get_or_set()` 方法自动管理缓存
  - **缓存功能特性**:
    - 支持设置过期时间
    - 支持自动缓存键生成
    - 支持缓存装饰器
    - 支持批量清除缓存
    - 内存存储，程序重启后清空（适合当前规模）
- **修改状态**: ✅ 已完成
- **测试结果**: 应用正常启动，缓存功能正常

---

### 38. 优化工单列表筛选条件 - 分组折叠
- **修改时间**: 2026-03-13
- **修改文件**: 
  - `templates/system/ticket/main.html`
  - `static/js/ticket/ticket_main.js`
- **修改内容**: 
  - **问题**: 工单列表页面有 20+ 个筛选条件，界面复杂，操作困难
  - **解决方案**: 将筛选条件分为"常用筛选"和"高级筛选"
  - **常用筛选**（默认显示）:
    - 工单标题/ID
    - 优先级
    - 序列号
    - 负责人
    - 客户/代理商
    - 工单状态
  - **高级筛选**（点击展开后显示）:
    - 产品信息：产品类型、产品二级分类、版本号、是否过保
    - 其他信息：详细描述、工程师工号
    - 服务与处理信息：响应方式、影响范围、接单时间、完成时间
    - 问题分析与处理过程：问题分类、模块标签、处理记录、处置方案
  - **添加功能**:
    - 添加"高级筛选"展开/收起按钮
    - 添加 CSS 样式美化筛选区域
    - 更新 JavaScript 事件绑定
  - **用户体验改善**:
    - 界面更简洁，常用筛选一目了然
    - 高级筛选按需展开，减少视觉干扰
    - 搜索和重置按钮放在常用筛选区域，操作更便捷
- **修改状态**: ✅ 已完成
- **测试结果**: 应用正常启动，筛选功能正常

---

### 39. 优化搜索区域显示方式
- **修改时间**: 2026-03-13
- **修改文件**: 
  - `templates/system/ticket/main.html`
  - `static/js/ticket/ticket_main.js`
- **修改内容**: 
  - **问题**: 搜索区域默认显示，占用页面空间
  - **解决方案**: 
    - 搜索区域默认隐藏
    - 点击表格工具栏的搜索图标显示/隐藏搜索区域
  - **具体修改**:
    - 在 CSS 中添加 `#searchArea` 默认隐藏样式
    - 添加 `#searchArea.show` 显示样式
    - 在 JavaScript 中添加搜索图标点击事件监听
    - 点击搜索图标时切换搜索区域的显示/隐藏状态
    - 显示搜索区域时自动重新渲染 Layui 表单元素
- **修改状态**: ✅ 已完成
- **测试结果**: 应用正常启动，搜索区域默认隐藏，点击搜索图标正常显示

---

### 40. 完善系统文档
- **修改时间**: 2026-03-13
- **修改文件**: 
  - 新建 `docs/API文档.md`
  - 新建 `docs/部署指南.md`
  - 新建 `docs/开发规范.md`
  - 新建 `docs/用户操作手册.md`
- **修改内容**: 
  - **API文档**: 详细说明所有API接口，包括请求方式、参数、返回值、示例
  - **部署指南**: 开发环境和生产环境部署步骤，环境变量配置，常见问题
  - **开发规范**: 代码规范、提交规范、数据库规范、API设计规范、安全规范
  - **用户操作手册**: 系统登录、工单管理、系统管理、个人中心、常见问题
- **修改状态**: ✅ 已完成
- **测试结果**: 文档创建完成，内容完整

---

## 待办事项
- [ ] 持续更新修改记录
- [ ] 定期备份数据库
- [ ] 测试所有功能是否正常

---

## 备注
- 修改状态标记：✅ 已完成 / ⏳ 进行中 / ❌ 失败
- 所有修改都需要经过测试验证后才能标记为完成
- 重要修改需要备份相关文件
