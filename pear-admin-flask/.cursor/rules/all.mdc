---
description: 
globs: 
alwaysApply: true
---

# Your rule content

- You can @ files here
- You can use markdown but dont have to
description: ✅ Interactive Loop with User Input
globs:

1. Sequential Thinking (深度思考与规划)
规则描述: 对于无法通过简单步骤完成的复杂、模糊或开放式问题，应优先使用 Sequential Thinking 工具。它通过引导式的思考链，帮助澄清问题、探索方案，并制定出逻辑清晰的执行计划。
触发条件:
任务无法通过一两个简单的指令完成。
任务描述宽泛或模糊，需要进行需求分析和澄清。
需要设计新的功能、架构或复杂的算法。
解决一个深层 Bug，需要追溯问题的根本原因。
使用流程:
启动: 识别出这是一个复杂问题，并启动 sequentialthinking 工具。
分解: 将大问题分解为一系列更小、更易于管理的子问题。
思考: 对每个子问题进行分析、提出假设，并规划解决方案。
验证: 在思考链的末端，对整个计划进行审查，确保其可行性和完整性。
执行: 根据最终形成的计划，调用其他工具执行具体操作。
2. Context7 (最新文档查询)
规则描述: 当需要获取关于某个软件库、API、框架或技术产品的准确、最新的官方文档时，应使用 Context7 系列工具。这确保了提供的代码示例和技术细节是权威且有效的。
触发条件:
用户询问“如何使用 X 库的 Y 功能？”
用户请求关于某个 API 的具体用法或示例代码。
需要验证某个函数的参数或用法时。
解决与第三方库相关的 Bug 或兼容性问题。
使用流程:
解析库 ID: 必须首先调用 mcp_context7_resolve-library-id 工具，将库名称（如 "React", "pandas"）解析为 Context7 的唯一 ID。
获取文档: 获取到库 ID 后，调用 mcp_context7_get-library-docs 工具，并传入库 ID 和用户关心的主题（如 "hooks", "routing"），来获取最相关的文档。
整合回答: 基于获取到的官方文档，整理出精准的答案或代码示例。
注意: 除非用户直接提供了 /org/project 格式的库 ID,否则严禁跳过第一步。
3. Playwright (浏览器自动化与调试)
规则描述: 当需要通过程序控制浏览器来完成网页交互、端到端测试、数据抓取或任何形式的 Web 自动化任务时，应调用 Playwright MCP 服务。该工具能够模拟真实用户操作，如打开页面、点击元素、填写表单、截屏,并捕获前端浏览器的控制台日志（console logs）用于调试。
触发条件:
用户请求与网站进行交互（如：“打开 CSDN 并搜索‘Python’”）。
需要从网页上抓取信息（如：“提取这个页面的所有新闻标题”）。
执行前端功能的自动化测试（如：“测试用户注册流程”）。
进行页面视觉验证（如：“截取首页的屏幕截图”）。
在进行前端操作时，需要捕获 console.log 或错误信息来辅助调试。
使用流程:
识别任务: 判断请求是需要通过浏览器自动化完成的任务。
设置监听: 如果需要捕获日志，应在执行具体操作前，先为页面设置 console 事件的监听器。
规划步骤: 将用户的指令分解为一系列具体的浏览器操作步骤。
调用工具: 激活 Playwright MCP 服务并顺序执行操作。
报告结果: 将执行结果（如操作是否成功、抓取到的数据、截图，以及捕获到的控制台日志）返回给用户。